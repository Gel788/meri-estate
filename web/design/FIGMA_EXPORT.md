# 🎨 Экспорт дизайна из Figma

## 📋 Что нужно для точной реализации дизайна

### Вариант 1: Figma API (Лучший вариант) ⭐

1. **Получить Figma File Key:**
   - Откройте файл в Figma
   - URL будет: `https://www.figma.com/file/FILE_KEY/...`
   - Скопируйте `FILE_KEY` из URL

2. **Получить Access Token:**
   - Figma → Settings → Account → Personal Access Tokens
   - Создайте новый токен
   - Скопируйте токен

3. **Экспортировать JSON:**
   ```bash
   # Используйте Figma API
   curl -H 'X-Figma-Token: YOUR_TOKEN' \
     'https://api.figma.com/v1/files/FILE_KEY' > design.json
   ```

### Вариант 2: Скриншоты всех экранов 📸

1. Экспортируйте каждый экран как PNG/JPG:
   - Выберите фрейм
   - Right Click → Export → PNG/JPG (2x или 3x)
   - Сохраните в `design/images/screenshots/`

2. Назовите файлы понятно:
   - `home-screen.png`
   - `property-detail.png`
   - `filter-modal.png`
   - и т.д.

### Вариант 3: Figma Dev Mode (Рекомендуется) 🚀

1. Откройте файл в Figma
2. Включите Dev Mode (⌘/Ctrl + Shift + D)
3. Скопируйте CSS/стили для каждого компонента
4. Сохраните в `design/docs/components.md`

### Вариант 4: Figma File URL 🔗

Просто дайте ссылку на файл:
- `https://www.figma.com/file/...`
- Я смогу описать структуру (но не смогу получить точные значения)

## 📦 Что экспортировать

### Обязательно:
- ✅ Цветовая палитра (Colors)
- ✅ Типографика (Text Styles)
- ✅ Компоненты (Components)
- ✅ Скриншоты всех экранов

### Желательно:
- ✅ Spacing/Grid система
- ✅ Иконки (SVG)
- ✅ Изображения
- ✅ Анимации/переходы

## 🎯 Формат для экспорта

### JSON структура (через API):
```json
{
  "document": {
    "id": "...",
    "name": "...",
    "type": "DOCUMENT",
    "children": [...]
  },
  "styles": {
    "colors": [...],
    "textStyles": [...]
  }
}
```

### Или просто:
1. Скриншоты всех экранов → `design/images/screenshots/`
2. Описание компонентов → `design/docs/components.md`
3. Цвета и стили → `design/docs/design-tokens.md`

## 📝 Инструкция

1. **Экспортируйте скриншоты:**
   - Все основные экраны
   - Сохраните в `design/images/screenshots/`

2. **Создайте файл с описанием:**
   - Цвета (hex коды)
   - Шрифты (размеры, веса)
   - Отступы (spacing)
   - Сохраните в `design/docs/design-tokens.md`

3. **Или дайте доступ к Figma:**
   - File → Share → Copy link
   - Или экспортируйте через API

---

**После экспорта** - я смогу реализовать дизайн 1 в 1! 🎨✨

