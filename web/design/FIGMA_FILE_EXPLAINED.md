# 📄 Про Figma файлы

## ❌ Почему нельзя использовать .fig файл напрямую:

Figma файл (`.fig`) - это **бинарный формат**, который:
- Требует специального софта (Figma Desktop App)
- Не читается как текст
- Содержит сжатые данные

## ✅ Что можно сделать:

### Вариант 1: Экспорт через Figma (Рекомендуется) 🎯

1. **Откройте файл в Figma:**
   - https://www.figma.com/design/jAKoIVQ7aWGEwJrHmgo6Kd/...

2. **Экспортируйте скриншоты:**
   - Выберите каждый Frame (экран)
   - Right Click → Export → PNG (2x)
   - Сохраните в `design/images/screenshots/`

3. **Скопируйте Design Tokens:**
   - Откройте панель Design (справа)
   - Скопируйте все цвета (hex коды)
   - Скопируйте текстовые стили
   - Заполните `design/docs/design-tokens.md`

### Вариант 2: Через Figma API (Автоматически) 🚀

Если файл **публичный** или у вас есть **токен**:

```bash
# С токеном
curl -H 'X-Figma-Token: YOUR_TOKEN' \
  'https://api.figma.com/v1/files/jAKoIVQ7aWGEwJrHmgo6Kd' \
  > assets/figma-export.json
```

### Вариант 3: Figma Dev Mode (Для компонентов) 💻

1. Откройте файл в Figma
2. Включите Dev Mode (⌘/Ctrl + Shift + D)
3. Выберите компонент
4. Скопируйте CSS из правой панели
5. Сохраните в `design/docs/components.md`

## 🎯 Самый простой способ:

1. **Скриншоты** всех экранов → `design/images/screenshots/`
2. **Цвета и стили** → `design/docs/design-tokens.md`

После этого я реализую дизайн 1 в 1! 🎨

---

**Примечание:** Если файл публичный, я могу попробовать получить данные через API без токена, но это не всегда работает.

