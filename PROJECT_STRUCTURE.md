# Структура проекта Meri Movs

## 📁 Организация файлов

```
Meri Estate/
├── Meri_EstateApp.swift          # Точка входа приложения
├── ContentView.swift              # Корневой View (содержит MainTabView)
│
├── Models/                        # 📦 Модели данных
│   ├── Property.swift            # Модель недвижимости, агента, координат
│   └── PropertyFilter.swift      # Модель фильтрации
│
├── ViewModels/                    # 🧠 Бизнес-логика
│   └── PropertyViewModel.swift   # Главный ViewModel (MVVM)
│
├── Views/                         # 📱 Экраны приложения
│   ├── MainTabView.swift         # TabBar контейнер (5 вкладок)
│   ├── HomeView.swift            # Главная страница
│   ├── SearchView.swift          # Поиск недвижимости
│   ├── FilterView.swift          # Расширенные фильтры
│   ├── FavoritesView.swift       # Избранные объекты
│   ├── MapView.swift             # Карта с маркерами
│   ├── CalculatorView.swift      # Калькулятор ипотеки
│   ├── PropertyDetailView.swift  # Детали объекта
│   ├── ContactAgentView.swift    # Контакты агента
│   │
│   └── Components/                # 🧩 Переиспользуемые компоненты
│       ├── PropertyCard.swift    # Карточка объекта (список)
│       └── FeaturedPropertyCard.swift  # Карточка избранного объекта
│
└── Assets.xcassets/              # 🎨 Ресурсы (иконки, цвета)
```

## 🔄 Поток данных

```
PropertyViewModel (ObservableObject)
    ↓
    ├── @Published properties: [Property]
    ├── @Published filteredProperties: [Property]
    ├── @Published favorites: Set<UUID>
    ├── @Published filter: PropertyFilter
    └── @Published sortOption: SortOption
    ↓
MainTabView (environmentObject: viewModel)
    ↓
    ├── HomeView
    ├── SearchView
    ├── FavoritesView
    ├── MapView
    └── CalculatorView
```

## 📲 Навигация

### TabBar (MainTabView)
- **Главная** (house.fill)
  - Категории (Все/Продажа/Аренда)
  - Избранные объекты (горизонтальный скролл)
  - Список всех объектов
  - → PropertyDetailView
  
- **Поиск** (magnifyingglass)
  - Строка поиска
  - Кнопка фильтров
  - Активные фильтры (чипы)
  - Результаты поиска
  - → FilterView (sheet)
  - → PropertyDetailView
  
- **Избранное** (heart.fill)
  - Список избранных объектов
  - → PropertyDetailView
  
- **Карта** (map.fill)
  - Интерактивная карта
  - Маркеры объектов с ценами
  - Карточка при выборе объекта
  - → PropertyDetailView
  
- **Калькулятор** (calculator.fill)
  - Параметры кредита (слайдеры)
  - Расчёт платежей
  - График распределения
  - Детальная разбивка

### Модальные окна (Sheets)
- **FilterView**: Расширенные фильтры
- **ContactAgentView**: Контакты и форма связи

## 🎯 Ключевые компоненты

### PropertyCard
```swift
// Используется в: HomeView, SearchView, FavoritesView
- Изображение с градиентом
- Бейджи (Новое, ★)
- Кнопка избранного
- Название и адрес
- Характеристики (комнаты, ванные, площадь)
- Цена и цена/м²
- Тип недвижимости
```

### FeaturedPropertyCard
```swift
// Используется в: HomeView (избранные объекты)
- Большое изображение (300x400)
- Градиентный оверлей
- Название, адрес
- Характеристики
- Цена
```

### PropertyDetailView
```swift
// Полная информация об объекте
- Галерея фотографий (TabView)
- Заголовок и статус
- Статистика (6 StatBox)
- Описание
- Особенности (Grid)
- Карта расположения
- Профиль агента
- Кнопка "Связаться с агентом"
```

## 💾 Модели данных

### Property
```swift
- id, title, address, city
- price, pricePerMeter, area
- rooms, bathrooms, floors, floor
- propertyType, status
- description, features, images
- coordinate, agent
- yearBuilt, isNew, isFeatured
- rating, views
```

### Agent
```swift
- id, name, photo
- phone, email
- rating, propertiesCount
- experience, description
```

### PropertyFilter
```swift
- searchText
- propertyType, status
- minPrice, maxPrice
- minArea, maxArea
- rooms, city
+ func matches(Property) -> Bool
```

## 🎨 Дизайн система

### Цвета
- **Основной**: Blue (акцент, кнопки, цены)
- **Успех**: Green (новые объекты, подтверждения)
- **Предупреждение**: Orange (избранное, важное)
- **Ошибка**: Red (переплата, удаление)
- **Фон**: .systemGroupedBackground
- **Карточки**: .systemBackground

### Шрифты
- **Title**: .system(size: 32, weight: .bold)
- **Title2**: .title2 + .bold
- **Headline**: .headline
- **Subheadline**: .subheadline
- **Body**: .body
- **Caption**: .caption

### Радиусы скругления
- Карточки: 16pt
- Кнопки: 12-20pt
- Чипы/Бейджи: 8-12pt

### Тени
- Основные карточки: opacity: 0.08, radius: 8
- Модальные окна: opacity: 0.15, radius: 10
- Subtle: opacity: 0.05, radius: 5

## 🚀 Расширяемость

### Легко добавить:
1. **Новые типы недвижимости** - добавить case в PropertyType
2. **Дополнительные фильтры** - расширить PropertyFilter
3. **Новые способы связи** - добавить кнопки в ContactAgentView
4. **Больше статистики** - добавить поля в Property
5. **Интеграция с API** - заменить loadMockData() в PropertyViewModel

### Точки интеграции:
- **PropertyViewModel.loadMockData()** → API вызов
- **ContactAgentView.sendMessage()** → Backend отправка
- **Images (systemName)** → Загрузка с сервера
- **Coordinate** → Реальные координаты из API

## 📝 Соглашения о коде

- **SwiftUI** синтаксис и best practices
- **MVVM** архитектурный паттерн
- **ObservableObject** для ViewModels
- **@EnvironmentObject** для передачи данных
- **Модульность** компонентов
- **Реюзабельность** UI элементов
- **Комментарии** на русском языке
- **Именование** на английском

---

Проект готов к расширению и интеграции с реальными данными! 🚀

